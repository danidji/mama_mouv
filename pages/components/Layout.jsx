import React from 'react';

const Layout = ({ welcome, main, contact }) => {

    return (
        <>
            {welcome}
            {main}
            {contact}
        </>
    )


};

export default Layout;